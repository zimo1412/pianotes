/**
 * Small constants decoupled from UI.
 * Scales rotation pool lives in locales (`scales.rotationPool`).
 */

/** Step size (minutes) for +/- buttons in the practice log modal. */
export const PRACTICE_LOG_DURATION_STEP = 5;

/** Archive JSON identity; import rejects mismatches. */
export const ARCHIVE_APP_ID = 'qinjianshouji';
export const ARCHIVE_VERSION = 2;

/** Initial empty plan before localStorage hydration. */
export const DEFAULT_BLOCKS = [];
